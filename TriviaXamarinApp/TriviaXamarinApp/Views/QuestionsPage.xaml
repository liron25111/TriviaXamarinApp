<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             x:Class="TriviaXamarinApp.Views.QuestionsPage" 
             NavigationPage.HasNavigationBar="False"
             x:Name="TheMainPage">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Login" Order="Secondary" Priority="0" Clicked="Login_Clicked"/>
        <ToolbarItem Text="register" Order="Secondary" Priority="0" Clicked="Register_Clicked"/>
        <ToolbarItem Text="User's page" Order="Secondary" Priority="1" Clicked="User_Clicked"/>
        <ToolbarItem Text="Edit page" Order="Secondary" Priority="1" Clicked="Edit_Clicked"/>
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout BackgroundColor="LightBlue" HorizontalOptions="Center" VerticalOptions="Center">
            <Label x:Name="qtext" Text="{Binding Atext}" TextColor="Black" FontSize="Large"></Label>
            <CollectionView ItemsSource="{Binding AnswerList}" x:Name="collec">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="4"></RowDefinition>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"></ColumnDefinition>
                            </Grid.ColumnDefinitions>
                            <Button x:Name="Answer" Grid.Column="1" Grid.Row="0" Text="{Binding Answer}" FontSize="20" Padding="0" TextColor="{Binding Color}" Command="{Binding Source={x:Reference TheMainPage}, Path= BindingContext.ChekCommand}" CommandParameter="{Binding}"></Button>
                            <Button x:Name="WrongAnswer" Grid.Column="1" Grid.Row="2" Text="{ Binding AnswersList[0]}" FontSize="20" Padding="0" TextColor="{Binding Color}" Command="{Binding Source={x:Reference TheMainPage}, Path= BindingContext.ChekCommand}" CommandParameter="{Binding}"></Button>
                            <Button x:Name="WrongAnswer2" Grid.Column="1" Grid.Row="3" Text="{Binding  AnswersList[1]}}" FontSize="20" Padding="0" TextColor="{Binding Color}" Command="{Binding Source={x:Reference TheMainPage}, Path= BindingContext.ChekCommand}" CommandParameter="{Binding}"></Button>
                            <Button x:Name="WrongAnswer3" Grid.Column="1" Grid.Row="4" Text="{Binding AnswersList[2]}" FontSize="20" Padding="0" TextColor="{Binding Color}" Command="{Binding Source={x:Reference TheMainPage}, Path= BindingContext.ChekCommand}" CommandParameter="{Binding}"></Button>

                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
                
            </CollectionView>
            <Label x:Name="msg" Text="{Binding Message}" HorizontalOptions="Center" TextColor="Red" FontSize="25"></Label>
            <Button x:Name="AddQ" Text="Add a question" Clicked="Add_Clicked" IsEnabled="{Binding Click}" ></Button>
            <Button x:Name="Next" Text="Next" Command="{Binding CreateQuestion}" FontSize="20" FontAttributes="None"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>